[project]
name = "sibr-market-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = [
    "scraping",
    "modeling",
    "cadastral",
    "api",
    "pipelines",
]
